server:
  port: 9060

spring:
  application:
    name: RESULTSERVICE

  datasource:
    url: jdbc:postgresql://localhost:5432/assignment
    username: postgres
    password: postgres

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka