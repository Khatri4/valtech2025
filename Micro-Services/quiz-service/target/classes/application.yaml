server:
  port: 9050

spring:
  application:
    name: QUIZSERVICE

  datasource:
    url: jdbc:postgresql://localhost:5432/assignment
    username: postgres
    password: postgres

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka